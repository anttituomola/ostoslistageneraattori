import React from 'react'

export const PortionHolder = (props) => {
    return <div style={{backgroundColor: "lightgrey", textAlign: "center"}}>
            <h2>{props.portion}</h2>
        </div>
}
